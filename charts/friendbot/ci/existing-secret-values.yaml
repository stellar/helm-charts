---
## Lint scenario: seed supplied through an existing Secret, which takes the
## other branch of the template -- the chart must not render a Secret itself.
friendbot:
  secret:
    existingSecret: "friendbot-seed"
